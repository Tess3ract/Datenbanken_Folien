\writeSectionFrame{\presentationTitle}{Implementierung einer Fluent API}
\begin{frame}[fragile]{Verwendung}
	\begin{exampleblock}{Main.java}
		\begin{lstlisting}
Task task = Task
    .named("Planetenoberflaeche scannen")
    .usingRobot("RX-17")
    .withPriority(Priority.HIGH)
    .after(Duration.ofMinutes(10))
    .repeatEvery(Duration.ofHours(2))
    .build();		
		\end{lstlisting}
	\end{exampleblock}
\end{frame}

\begin{frame}[fragile]{Implementierung}
	\begin{exampleblock}{Task.java}
		\begin{lstlisting}[basicstyle=\tiny]
public final class Task {
    private final String name;
    private final String robotId;
    private final Priority priority;
    private final Duration delay;
    private final Duration repeatInterval;

    private Task(Builder builder) {
        this.name = builder.name;
        this.robotId = builder.robotId;
        this.priority = builder.priority;
        this.delay = builder.delay;
        this.repeatInterval = builder.repeatInterval;
    }

    public static Builder named(String name) {
        return new Builder(name);
    }
 }
		\end{lstlisting}
	\end{exampleblock}
\end{frame}

\begin{frame}[fragile]{Implementierung}
	\begin{exampleblock}{Task.java}
		\begin{lstlisting}[basicstyle=\tiny]
public static class Builder {
    private final String name;
    private String robotId;
    private Priority priority = Priority.NORMAL;
    private Duration delay = Duration.ZERO;
    private Duration repeatInterval;

    private Builder(String name) {
        this.name = name;
    }

    public Builder usingRobot(String robotId) {
        this.robotId = robotId;
        return this;
    }

    public Builder withPriority(Priority priority) {
        this.priority = priority;
        return this;
    }
}
		\end{lstlisting}
	\end{exampleblock}
\end{frame}